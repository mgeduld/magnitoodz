export enum ErrorMessage {
  invalidCredentials = 'Invalid credentials',
  alreadyRegistered = 'Display name and/or email already registered',
  invalidUserId = 'Invalid user id ',
  invalidCriteria = 'Invalid criteria in '
}
